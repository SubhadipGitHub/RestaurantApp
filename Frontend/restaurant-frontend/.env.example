# Server-only. Read at BUILD time by next.config.mjs to target the API proxy.
# Changing it requires a redeploy, not just an env var save.
BACKEND_URL=http://127.0.0.1:8000

# Which restaurant this deployment shows.
NEXT_PUBLIC_RESTAURANT_ID=REST_001
